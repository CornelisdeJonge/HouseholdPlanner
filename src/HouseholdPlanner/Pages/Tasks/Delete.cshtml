@page "{id:int}"
@model HouseholdPlanner.Pages.Tasks.DeleteModel
@{
    ViewData["Title"] = "Delete Task";
}

<h1 class="mb-4 text-xl font-semibold">Delete Task</h1>

<div class="panel rounded-xl p-4">
    <div class="mb-4">
        <p class="text-muted text-sm">Are you sure you want to delete this task?</p>

        <div class="mt-3 rounded-md border border-red-200 bg-red-50 p-3 text-sm text-red-800">
            <div class="font-semibold">@Model.TaskItem.Name</div>
            @if (!string.IsNullOrWhiteSpace(Model.TaskItem.Description))
            {
                <div class="mt-1 text-xs">@Model.TaskItem.Description</div>
            }
        </div>
    </div>

    <form method="post" class="flex items-center gap-3">
        <button type="submit" class="btn btn-danger">
            Delete
        </button>
        <a asp-page="./Index" class="btn btn-secondary">
            Cancel
        </a>
    </form>
</div>